package linkedlist;

public class LinkedList {
/*Design and implement a singly linked list. 
	addition 
    removal of elements,
    querying of the head element, 
    obtaining the nth element and 
    computing the length of the list.
    Your list only needs to work with integers.*/
	
	
	
	
	public static void main (String[] args){
		Node head = new Node(2,null);
		System.out.println(head);
	}

}
